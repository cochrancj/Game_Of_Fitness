<div class="gameboard" ng-app="Game-Of-Fitness-App">

  <div class="wod" ng-controller="WorkoutsController">

    <header>
      <div>
        <h1>Workout Of The Day</h1>
      </div>
    </header>



    <button ng-click="shuffle(cards)">Re-Shuffle</button>

    <!-- <div class="pure-g">
      <div class="pure-u-1-3"> -->
        <div class="container">
          <ul class="cards">
            <li>
              <span id="card" ng-repeat="card in cards | orderBy:random" ng-hide="custom">
              <!-- <span id="card" ng-repeat="card in cards | orderBy:random" ng-click="toggleClass($event, 'is-active')"> -->
              <!-- <h4> Card Name: {{ card.name }}</h4>
              <p> Mana: {{ card.mana }}</p> -->
              <img    ng-src='/assets/{{card.img}}'/>

              <button id="done" ng-click="toggleCustom()">Complete!</button>


              <!-- <button id="btn" ng-click="toggleClass($event, 'is-active')">Change Class</button> -->


              </span>

            </li>
          </ul>
        </div>
      </div>
    <!-- </div>
  </div> -->

</div>
